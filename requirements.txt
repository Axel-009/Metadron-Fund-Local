# =============================================================================
# Metadron Capital — Python Dependencies
# Generated by scanning all imports across engine/, core/, config/, and root .py
# =============================================================================

# ---------------------------------------------------------------------------
# Core API
# ---------------------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
starlette>=0.40.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
sqlalchemy>=2.0.30
alembic>=1.13.0
psycopg2-binary>=2.9.9
redis>=5.0.0

# ---------------------------------------------------------------------------
# Data & Market Data
# ---------------------------------------------------------------------------
pandas>=2.2.0
numpy>=1.26.0
yfinance>=0.2.40
openbb>=4.2.0
alpaca-py>=0.30.0
httpx>=0.27.0
aiohttp>=3.9.0
websockets>=12.0

# ---------------------------------------------------------------------------
# ML & Quant
# ---------------------------------------------------------------------------
scikit-learn>=1.5.0
scipy>=1.13.0
torch>=2.3.0
xgboost>=2.0.0
hmmlearn>=0.3.0
transformers>=4.40.0
pysr>=0.19.0

# ---------------------------------------------------------------------------
# Monitoring & Ops
# ---------------------------------------------------------------------------
prometheus-client>=0.20.0
python-dotenv>=1.0.0
rich>=13.7.0
PyYAML>=6.0.1

# ---------------------------------------------------------------------------
# Auth & Security
# ---------------------------------------------------------------------------
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# ---------------------------------------------------------------------------
# LLM / AI Bridges
# ---------------------------------------------------------------------------
openai>=1.30.0
anthropic>=0.28.0
airllm>=2.10.0

# ---------------------------------------------------------------------------
# PDF / Reporting
# ---------------------------------------------------------------------------
markdown>=3.6
weasyprint>=62.0
graphifyy>=0.3.0
sse-starlette>=1.6.5
python-socketio[client]>=5.11.0

# ---------------------------------------------------------------------------
# OPEN JARVIS — Voice + text assistant (STT: faster-whisper, TTS: kokoro/piper)
# Install openjarvis[speech] to enable full voice pipeline
# ---------------------------------------------------------------------------
openjarvis>=0.1.1
# openjarvis[speech]>=0.1.1  # Uncomment to enable faster-whisper + kokoro TTS
